name = "yairb-booking"
main = "src/index.ts"
compatibility_date = "2024-09-23"
workers_dev = true
preview_urls = true

routes = [
  { pattern = "letsilluminate.co/api/*", zone_name = "letsilluminate.co" }
]

[vars]
# Switch each provider independently: 'mock' or its real value ('supabase'/'resend'/'google'/'stripe'/'turnstile')
REPOSITORY_MODE = "mock"
EMAIL_MODE      = "resend"
CALENDAR_MODE   = "google"
PAYMENTS_MODE   = "mock"
ANTIBOT_MODE    = "mock"
SITE_URL = "https://letsilluminate.co"
SESSION_ADDRESS = "Via Example 1, 6900 Lugano"
SESSION_MAPS_URL = "https://maps.google.com/?q=Lugano+Switzerland"
GOOGLE_CALENDAR_ID = "yairilluminate@gmail.com"
GOOGLE_TOKEN_URI   = "https://oauth2.googleapis.com/token"
TIMEZONE           = "Europe/Zurich"

# Local-only overrides (used by `wrangler dev --env local`)
[env.local.vars]
SITE_URL = "http://localhost:8080"

# Secrets — set via `wrangler secret put <NAME>` for prod, never commit values:
# SUPABASE_URL
# SUPABASE_SERVICE_ROLE_KEY
# RESEND_API_KEY
# GOOGLE_OAUTH_CLIENT_ID
# GOOGLE_OAUTH_CLIENT_SECRET
# GOOGLE_OAUTH_REFRESH_TOKEN
# GOOGLE_CLIENT_EMAIL         ← service account email
# GOOGLE_PRIVATE_KEY          ← service account private key (PEM)
# STRIPE_SECRET_KEY
# STRIPE_WEBHOOK_SECRET
# TURNSTILE_SECRET_KEY
# JOB_SECRET

[observability.logs]
enabled = true

# [triggers] — re-enable when moving to production (requires paid plan for >5 crons account-wide)
# crons = ["*/5 * * * *", "*/15 * * * *", "*/30 * * * *", "0 * * * *"]
