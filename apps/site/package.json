{
  "name": "illuminate-site-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest --run --environment=jsdom"
  },
  "devDependencies": {
    "jsdom": "^24.1.3",
    "vitest": "^1.6.1"
  }
}
