{
  "name": "api-booking",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env local --port 8787",
    "deploy": "wrangler deploy --env \"\"",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "delete:client-prefix": "node ./scripts/delete-client-prefix.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240605.0",
    "@vitest/coverage-v8": "^2.0.5",
    "vitest": "^2.0.5",
    "typescript": "^5.5.0",
    "wrangler": "^4.71.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.98.0",
    "resend": "^4.8.0"
  }
}
